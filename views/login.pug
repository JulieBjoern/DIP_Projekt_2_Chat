extends layout

block content
    h1 Her kan du logge ind
    form(action="/users/login", method="post")
        input(type="text", name="username", placeholder="brugernavn")
        input(type="password", name="password", placeholder="kodeord")
        button(type="submit") Login
